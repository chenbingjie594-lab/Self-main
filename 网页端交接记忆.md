# Self-main 项目交接记忆

> 用途：将本文件上传或粘贴给新的网页端对话，使其快速恢复项目上下文。更新时间：2026-09-01。

## 1. 用户的核心目标

本项目研究高分辨率工业图像中的微小瑕疵生成。主要目标是提升自建 `Plastic_Bomo` 数据集的生成效果与下游 YOLO 检测性能；公开数据集 DeepPCB 只用于辅助证明方法具有泛化性，不应取代自建数据集主线。

用户明确要求：

- 重点是提出真正的结构或训练创新，不是单纯调参。
- 一切以最终效果提升为优先，训练成本暂不考虑，除非实现不了。
- 新模块应从原始 SD2 Inpainting 权重重新训练相同步数后与 Baseline 公平比较，不能长期依赖 Baseline checkpoint 做后处理微调。
- 生成效果不能过好到没有创新空间，也不能差到无法改进。
- 下游实验必须保持相同真实验证集，合成图只能加入训练集。
- 自建数据的 Mask 是根据 bbox 缩小得到的粗区域，不是精确分割 Mask；生成在 Mask 中心没有问题，因为中心对应缺陷中心。
- 4096×1024 原图里的缺陷是真正的微小目标；512×512 局部图中的缺陷视觉上并不小，因此下游检测必须把生成缺陷按真实物理尺度回贴到高分辨率背景。

## 2. 数据与服务器路径

项目目录：

```text
/mnt/sda/cbj/ImageGeneration/Self-main
```

SD2 Inpainting：

```text
/mnt/sda/cbj/ImageGeneration/Self-main/sd2-community/stable-diffusion-2-inpainting
```

自建生成数据：

```text
/mnt/sda/cbj/dataset/Plastic_Bomo/Plastic_Bomo_split70_seed42/train
```

当前纯真实 YOLO 数据：

```text
/mnt/sda/cbj/dataset/Plastic_Bomo/GLRR_detection_no_hgrr_plus_black35_s42/real_only
```

高分辨率正常背景可使用：

```text
/mnt/sda/cbj/dataset/Plastic_Bomo/Plastic_Bomo_split70_seed42/train/Plastic_Bomo/train/good
```

本机项目：

```text
C:\MyFile\Study\代码\Self-main
```

本机额外背景曾位于：

```text
C:\MyFile\Study\dataset\Plastic_Bomo\all-cut-empty
```

类别固定为：

```text
0: Flash point
1: Big black spots
```

Scar 已从当前两类检测实验中移除。新增 35 张 Black instance 已统一图片格式、生成 YOLO 框并以 25/10 划分后加入真实检测数据，用于缓解黑点样本不足。

## 3. Baseline 与公平实验原则

Baseline 是去掉所有创新模块后的 SD2 Inpainting 训练版本，不是 MAGIC 原始完整模块版。Baseline 与创新模型应满足：

- 都从原始 SD2 Inpainting 初始化。
- 每个缺陷类别使用相同训练步数，当前主要设置为 2000 steps。
- 相同数据划分、seed、学习率、batch、梯度累积和推理条件。
- 下游 YOLO 均从同一个 `yolo11s.pt` 初始化。
- Real-only 和增强组使用逐文件相同的真实验证集。
- 不允许把合成图加入最终验证集来“平衡类别”。

曾经的 3-shot 实验因为样本太少，出现背景失真、缺陷无形状和全黑图等问题，已经放弃；当前使用普通 70% 划分。

## 4. 已探索和放弃的方向

### 4.1 UCSG

先后尝试推理引导、校准器和 joint 训练，但生成尺度反复过大或过小，且早期设计主要围绕 Baseline 输出进行校正，没有直接利用 GT 训练扩散过程。该方向已经停止，不应恢复旧 UCSG 文件。

### 4.2 RDA

RDA 曾有部分生成指标改善，v4 相对 Baseline 较稳定，并做过 E1 loss-only 消融与 DeepPCB 辅助验证。但后续结构越来越复杂，最终不再作为主要创新点。历史参数和结果仍可保留用于对照。

### 4.3 CARF

CARF-only 一度比 RDA 视觉效果更好，也做过 DeepPCB 和下游检测实验。但增强后 Precision、Recall、mAP 的提升不稳定，Big black spots 有时不如 Baseline。CARF v3/v4 继续调整后，问题被判断为生成质量而不只是高分辨率回贴方式，因此不再作为当前核心创新点。

### 4.4 DHFG

DHFG 试图用冻结 YOLO 教师在扩散训练中提供检测反馈。v1-v5 先后尝试类别原型、核心—背景特征差、极性感知分布校准和反事实差分反馈，但出现明显教师捷径：Flash point 形成“白点带黑影”，Big black spots 大量缺失。多次修改外观几乎不变，说明检测反馈直接写入生成外观的方向不稳定。DHFG 已标记为历史探索，不再作为第二创新点。

## 5. 当前创新点一：MSDF

MSDF（Multi-Scale Defect Fidelity）用于缓解微小缺陷经过 VAE/U-Net 多次下采样后形态丢失的问题。它采用多尺度高分辨率局部参考注入、尺度/时间门控、区域重建、结构、背景和尺度约束，并在训练和推理阶段都启用。

当前主要版本：

```text
model/msdf_v3_sd2_2000_s42
configs/msdf.json
results/msdf_v3_sd2_2000_s42
```

MSDF v1/v2 曾出现：

- metadata 初始化顺序错误。
- fp16 中 NaN 导致几何尺寸转换失败。
- 推理缺少 `reference_pixels`。
- 非有限梯度。

上述问题已经修复。MSDF v3 已完成生成和初步下游检测，当前作为第一创新点保留。

完整历史说明见：

```text
改动点/新创新点-MSDF与DHFG.md
```

该文档顶部已经注明 DHFG 是历史方案，MSDF 仍是第一创新点。

## 6. 当前创新点二：DASR

DASR（Detector-Aware Synthetic Rebalancing，检测感知的合成缺陷重平衡）是当前第二创新点，用于解决“生成图看起来合理，但全部加入训练后检测性能反而下降”的问题。

核心思想：

1. 只从真实训练集统计缺陷亮/暗极性、颜色、框面积和长宽比先验，不读取最终验证集。
2. 对生成图与对应 normal 做差，提取实际缺陷残差；粗 Mask 只限定搜索区域，不直接作为 YOLO 框。
3. 过滤缺陷未生成、背景泄漏、支持域过大、属性异常和极性错误的候选。
4. 用纯真实训练数据做三折 OOF，统计各类别与尺度区间的漏检程度，形成检测薄弱性权重。
5. Real-only 探针为候选计算适度困难度；不是越难越好，优先中等置信度样本。
6. 使用特征空间最远点选择避免大量近重复样本。
7. 按真实框分布把残差回贴到 4096×1024 正常背景，YOLO 框由回贴后的真实支持域重新计算。
8. 两类使用固定相同配额；合成图只进入训练集。

DASR 不需要再次训练扩散模型。它复用已训练的 MSDF 多 seed 输出，需要训练三折 OOF 检测器、Real-only 候选探针和最终 Real-only/Real+DASR 对照模型。

相关实现：

```text
tools/create_dasr_yolo_folds.py
tools/profile_dasr_weakness.py
tools/build_dasr_detection.py
改动点/创新点二-DASR.md
```

`inference.py` 已修改：显式传入 `--seed` 时命令行 seed 优先于 JSON 配置 seed。此前配置会强制覆盖命令行，导致所谓多 seed 仍可能重复。

## 7. DASR 已完成的本地验证

- 所有新增脚本和 `inference.py` 通过 `py_compile`。
- 138 张真实训练图被分成三折：91/47、92/46、93/45；每张图恰好被 OOF 留出一次。
- Real-only 与 Real+DASR 的真实验证集共有 92 个图像/标注文件，逐文件 SHA-256 完全一致。
- 冒烟生成的两类 YOLO 框均合法且位于 `[0,1]`。
- 回贴后的示例框宽约占 4096 原图的 0.56%–0.59%，恢复为真实微小目标尺度。
- 在每类只读取前 5 个 MSDF v3 候选的严格测试中，仅有 3 个 Flash 和 1 个 Black 通过质量门控。这说明正式实验必须生成多个独立 seed，尤其是 Black spots，不能复制同一缺陷凑数量。
- 最终入选样本默认使用不同高分辨率背景；候选池评分阶段可以循环背景，不会错误要求几百张背景图。
- 候选不足时程序会输出每个类别的有效数量及各淘汰原因。

## 8. 当前下一步

当前应优先完成 DASR 的核心验证，而不是继续设计第三个模块：

1. 用 MSDF v3 checkpoint 生成 8 个左右不同 seed 的候选池。
2. 训练纯真实 Real-only 探针。
3. 创建三折 OOF 并训练三个 OOF YOLO11s。
4. 生成 `dasr_weakness_s42.json`。
5. 先构建每类 40 张的 `GLRR_DASR_40_40_s42`，与之前 unique40 数量保持一致。
6. 从同一个 `yolo11s.pt` 分别训练 Real-only 和 Real+DASR。
7. 比较总体 Precision、Recall、mAP50、mAP50-95，以及两个类别各自的 AP50-95。
8. 若有效，再扩展到 80+80 和 DeepPCB 辅助泛化验证。

完整服务器命令已经写入：

```text
改动点/创新点二-DASR.md
```

## 9. DASR 最小核心实验

主实验：

```text
Real-only
Real + MSDF（不筛选，直接加入固定数量）
Real + DASR（相同固定数量）
```

必须保证三组真实验证集完全一致。主表报告：

- Precision
- Recall
- mAP50
- mAP50-95
- Flash point AP50-95
- Big black spots AP50-95

必要消融仅保留三项：

- 去掉质量门控。
- 去掉检测薄弱性权重。
- 去掉多样性选择。

所有消融使用相同合成配额。不要重新堆大量调参实验。

## 10. 评价指标理解

生成质量曾使用：

- KID：生成分布与真实分布差异，越低越好。
- IC-LPIPS：缺陷区域或条件一致性的感知距离，具体方向需按实现定义解释。
- Local-KID、BG-LPIPS 后来发现提升不明显，不作为核心结果。

下游检测使用经典 YOLO 指标：

- Precision：预测为缺陷的框中有多少是真的。
- Recall：真实缺陷中有多少被检出。
- mAP50：IoU=0.5 下的平均 AP。
- mAP50-95：IoU 0.5 到 0.95 的平均 AP，更严格、更适合作为主指标。

不应只因为某一个指标上升就宣布有效。当前优先判断总体 mAP50-95、两类 AP50-95 和 Recall 是否稳定改善，同时观察 Precision 是否因合成伪影明显下降。

## 11. 环境与显卡注意事项

生成环境通常为：

```bash
conda activate magic
```

检测环境通常为：

```bash
conda activate yolo11
```

`CUDA_VISIBLE_DEVICES=2` 后，程序内部只看到一张逻辑 GPU 0，所以日志或报错显示 `GPU 0` 是正常映射，不代表使用物理 0 号卡。YOLO 命令建议写：

```bash
CUDA_VISIBLE_DEVICES=2 yolo ... device=0
```

不要同时写 `CUDA_VISIBLE_DEVICES=0` 和 `device=2`，这会造成映射混乱。

24GB 显卡训练 SD2 时使用：batch 1、gradient accumulation 4、fp16、gradient checkpointing；显存中若已有其他进程，即便指定卡看似空闲也可能 OOM，应先通过 `nvidia-smi` 核对 PID 和物理 UUID。

## 12. 对网页端助手的要求

- 不要重新建议 3-shot。
- 不要把公开数据集当作研究主线。
- 不要通过修改最终验证集或加入合成验证图制造提升。
- 不要复制相同生成缺陷凑样本数。
- 不要再恢复 UCSG 或 DHFG 的旧方向，除非用户明确要求复盘。
- 判断创新是否有效时应先看公平指标，再结合视觉质量，不要只凭单张截图。
- 用户要求修改代码前，可以讨论方案；一旦明确要求修改，应实现并进行静态检查和小规模冒烟验证。
- 当前最重要任务是完成 DASR 40+40 的自建数据集下游验证。
